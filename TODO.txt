- Recheck pyproject-nix/build-system-pkgs' uv.lock (currently scikit-build-core 0.12.2); when it supports 1.x, revisit our <1 cap and metadata syntax.

- When scikit-build-core editable rebuild is no longer experimental, consider enabling it.

- When nixpkgs' gersemi catches up with verson >= 0.25.4, remove our custom override.

- When dprint merges PR #1207, move the dprint-*.json configs to config/. Probably dprint > 0.55.x.

- Nix formatting is routed through dprint and the official nixfmt.
  Aggregate format/check commands skip it when unavailable, while explicit Nix commands require it and `nix develop` provides the canonical formatter.
  Review the unstable Just feature use in just/format-nix.just once those features are stabilized.
